{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun astro dev && bunx wrangler pages dev ./dist",
    "start": "bun astro dev && bunx wrangler pages dev ./dist",
    "build": "astro build",
    "preview": "astro build && bunx wrangler dev",
    "deploy": "bun astro build && bunx wrangler pages deploy ./dist",
    "deploy:prod": "bunx wrangler pages deploy ./dist",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.5.4",
    "@astrojs/starlight": "^0.34.3",
    "@tailwindcss/vite": "^4.1.3",
    "astro": "^5.6.1",
    "sharp": "^0.34.2",
    "starlight-llms-txt": "^0.5.1",
    "starlight-videos": "^0.3.0",
    "tailwind-merge": "^3.3.0",
    "tailwindcss": "^4.1.3",
    "wrangler": "^4.20.0"
  },
  "devDependencies": {
    "tw-animate-css": "^1.3.4"
  },
  "trustedDependencies": [
    "sharp"
  ]
}
